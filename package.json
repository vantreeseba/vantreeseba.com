{
  "name": "vantreeseba.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -- eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "dotenv-cli": "^11.0.0"
  },
  "dependencies": {
    "@11ty/eleventy-fetch": "^5.1.2"
  }
}
